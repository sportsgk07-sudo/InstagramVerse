import { createMetadata } from "@/lib/seo/metadata";

export const metadata = createMetadata({
  title: "Contact Us — InstagramVerse",
  description: "Get in touch with the InstagramVerse team for support, feedback, or partnership inquiries.",
  path: "/contact",
});

export { default } from "./contact-form";
